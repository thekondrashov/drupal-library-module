; ------------------------------------------------------------------------------
; Параметры файла .Info
; http://azbukaweb.ru/telling-drupal-about-your-module
; Шаблон META-информации о модуле
; https://github.com/topsitemakers/drupal7-boilerplate/blob/master/boilerplate.info
; Writing .info files (Drupal 7.x)
; http://drupal.org/node/542202
; ------------------------------------------------------------------------------


; ------------------------------------------------------------------------------
; name (обязательный)
; Отображаемое имя модуля. Оно должно соответствовать стандарту заглавных букв Drupal: только первая буква первого слова должна быть заглавной. ("Тестовый модуль", но не "тестовый модуль" или "Тестовый Модуль").
; ------------------------------------------------------------------------------
name = Библиотека

; ------------------------------------------------------------------------------
; description (обязательный)
; Короткое (желательно в одну строку) описание, которое подскажет администратору сайта, что делает этот модуль, на странице администрирования модулей. Ограничение для размер поля - 255 символов. Описание может содержать ссылки на документацию и первоисточники.
; ------------------------------------------------------------------------------
description = Модуль библиотеки для <a href="http://www.ump.mv.ru/" target="_blank">ОАО «УМЗ»</a>.

; ------------------------------------------------------------------------------
; core (обязательный)
; Версия Drupal, для которой написан этот модуль. Для Drupal 7, например, это будет 7.x. Модуль не может указывать второстепенную версию ветки Drupal. Т.е., указание 7.x - будет корректно, а 7.4 - нет.
; ------------------------------------------------------------------------------
core = 7.x

; ------------------------------------------------------------------------------
; version (опциональный)
; версия модуля
; ------------------------------------------------------------------------------
version = 7.x-1.x-dev

; ------------------------------------------------------------------------------
; package (опциональный)
; Если модуль имеет параметр package, то на странице admin/build/modules он будет объединен с другими модулями в одноименную категорию. Если параметр package не задан, то он просто будет в категории 'Other' (или 'Другой' - в локализованной версии). Вполне нормальным будет не назначать package для модуля. Обычно package используют для группировки совместно распространяемых модулей или предполагающих совместное использование.
; ------------------------------------------------------------------------------
package = Custom

; ------------------------------------------------------------------------------
; files (опциональный)
; Drupal поддерживает регистрацию динамически подгружаемого кода. Когда модуль подключается, Drupal будет пересканировать все файлы и индексировать все классы и интерфейсы, которые найдет. Классы будут автоматически загружаться интерпретатором PHP при первом обращении к ним.
; ------------------------------------------------------------------------------
; files[] = boilerplate.test

; ------------------------------------------------------------------------------
; dependencies (опциональный)
; Показыват зависимость между модулями. Если модуль требует для своей работы включения других модулей, то их имена файлов перечисляют в следующем формате:
; dependencies[] = taxonomy
; dependencies[] = comment
; Drupal не активизирует модуль до тех пор пока не будут включены все модули, от которых он зависит.
; ------------------------------------------------------------------------------
; dependencies[] =

; ------------------------------------------------------------------------------
; Scripts
; ------------------------------------------------------------------------------
; scripts[] = js/boilerplate.js

; ------------------------------------------------------------------------------
; Stylesheets
; ------------------------------------------------------------------------------
; stylesheets[all][] = css/boilerplate.css

; ------------------------------------------------------------------------------
; Other
; ------------------------------------------------------------------------------
; Configuration URL
; configure = admin/config/boilerplate/settings
